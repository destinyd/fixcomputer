<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=0.3, maximum-scale=0.6, minimum-scale=0.1">
    <%= display_meta_tags default_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <!--[if IE 6]>
    <%= javascript_include_tag "iepng" %>
    <%= stylesheet_link_tag "ie6", :media => "all" %>
    <![endif]-->  
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
    <!-- handles the jquery cycle slider -->
    <script>
      var loading;
      $(document).ready(function() {
        $('form').submit(
          function(){
            loading = $('<div class="loading"><%= j image_tag "loading.gif",alt:"loading",title:"loading" %></div>').dialog({modal: true,title: '信息提交中，请稍等'});
          }
        )
    });
    </script>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">
    <!--[if IE 6]>
    <div id="notice_ie6">
      使用IE6浏览器可能导致无法正常使用本网站，请使用更高版本的IE、<a href="http://firefox.com.cn/">火狐</a>或Chrome浏览器访问。
    </div>
    <![endif]-->  
    <%#= render 'layouts/messages' %>
    <%= yield %>
  </body>
</html>
